[build]
builder = "dockerfile"

[deploy]
startCommand = "python sync_obsidian.py python scheduler.py"
restartPolicyType = "always"
restartPolicyMaxRetries = 10
healthcheckPath = ""
healthcheckTimeout = 300
