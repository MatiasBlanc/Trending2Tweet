Actúa como un Ingeniero Backend experto en Python. Tu tarea es escribir el script completo para un bot de automatización que conecte la API de GitHub con la API de X (Twitter).

RESTRICCIONES ESTRICTAS DE SALIDA (CRÍTICO PARA CONSERVAR TOKENS):

    Cero texto de relleno. No saludes, no te despidas, no escribas introducciones ni conclusiones.

    No incluyas instrucciones de instalación de entorno.

    Toda explicación técnica necesaria debe ir exclusivamente como comentarios cortos dentro del código (#).

    Entrega un codigo limpio y listo para ejecución.

    Ademas crea archivos modulares y usa .env para las apikeys que necesites

ESPECIFICACIONES TÉCNICAS DEL SISTEMA:

    Lenguaje: Python 3.10+

    Dependencias clave: tweepy (X API v2), requests (GitHub REST API), openai (o la librería de la IA que prefieras usar), python-dotenv.

    Lógica principal:

        Descubrimiento: Usar la Search API de GitHub para buscar los repositorios con más estrellas creados en el ultimo mes (ej. q=created:>YYYY-MM-DD&sort=stars&order=desc).

        Filtrado: Tomar el repositorio top que no haya sido publicado antes. Extraer su nombre, descripción, lenguaje principal, estrellas y URL.

        Generación con IA: Enviar esos datos a la API del LLM con un system prompt que le ordene redactar un tweet directo, técnico, sin hashtags innecesarios ni emojis excesivos, explicando por qué este repositorio es valioso.

        Publicación: Enviar el texto generado a la API de X.

    Persistencia de estado: Implementa un sistema ligero usando un archivo state.json local para almacenar los IDs o URLs de los repositorios ya procesados, evitando tuitear duplicados en el futuro.

    Gestión de secretos: Todas las credenciales (GitHub Token, X API Keys, LLM API Key) deben cargarse desde un archivo .env.

    Control de fallos: Implementa bloques try/except para manejar respuestas HTTP 429 (Rate Limits) y errores de la API del LLM sin romper la ejecución.

Genera el código ahora respetando las restricciones de salida.
